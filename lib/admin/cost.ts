// Single source for cost thresholds across the admin console
// (Overview needs-attention, the cockpit health flag, and the Cost tab).
export const THB_PER_USD = 36;
// A user whose AI cost crosses this (in THB, over the lookback window) gets flagged.
export const COST_ALERT_THB_7D = 200;
